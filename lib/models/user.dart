class User {
  DateTime createdAt;
  DateTime disabledAt;
  String email;
  int id;
  String role;

  User({
    this.createdAt,
    this.disabledAt,
    this.email,
    this.id,
    this.role,
  });
}
